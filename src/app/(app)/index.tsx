import { View, Text } from "react-native";

export default function AppHome() {
  return (
    <View className="flex-1 bg-navy-900 items-center justify-center">
      <Text className="text-white text-lg">Logged in — Feed/Tabs coming next</Text>
    </View>
  );
}